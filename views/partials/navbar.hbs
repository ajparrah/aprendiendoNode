<div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
<header class="masthead mb-auto">
  <div class="inner">
    <h3 class="masthead-brand">Cover</h3>
    <nav class="nav nav-masthead justify-content-center">
      <a class="nav-link {{#if (isPossibleAddActiveClass "/" url) }} active {{/if}}" href="/">Home</a>
      <a class="nav-link {{#if (isPossibleAddActiveClass "/contact" url) }} active {{/if}}" href="/contact">Contact</a>
    </nav>
  </div>
</header>